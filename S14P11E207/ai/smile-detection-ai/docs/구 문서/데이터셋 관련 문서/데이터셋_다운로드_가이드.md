# 데이터셋 다운로드 가이드

**작성일**: 2026-01-14
**목적**: 학습 데이터셋 다운로드 및 설정 방법

---

## 📦 필요한 데이터셋

### 필수 (우선순위 ⭐⭐⭐⭐⭐)

1. **GENKI-4K** - 웃음 감지 전용 (Head 1)
2. **FER2013** - 표정 분류 (Head 2)

### 선택 사항

3. **SMILES** - 웃음 감지 보조
4. **CelebA** - 웃음 감지 대규모 (비상업적 연구만)

---

## 🔧 사전 준비: Kaggle API 설정

### 1단계: Kaggle 계정 생성

1. [Kaggle](https://www.kaggle.com) 접속
2. 회원가입 (무료)

### 2단계: API 토큰 생성

1. [Kaggle Settings](https://www.kaggle.com/settings/account) 접속
2. "Create New API Token" 클릭
3. `kaggle.json` 파일 자동 다운로드

### 3단계: API 토큰 설치

#### Windows

```bash
# .kaggle 폴더 생성
mkdir %USERPROFILE%\.kaggle

# kaggle.json 복사
copy Downloads\kaggle.json %USERPROFILE%\.kaggle\kaggle.json
```

또는 수동으로:
- `kaggle.json` 파일을 `C:\Users\사용자명\.kaggle\` 폴더에 복사

#### Linux / Mac

```bash
# .kaggle 폴더 생성
mkdir -p ~/.kaggle

# kaggle.json 복사
cp ~/Downloads/kaggle.json ~/.kaggle/kaggle.json

# 권한 설정 (중요!)
chmod 600 ~/.kaggle/kaggle.json
```

### 4단계: Kaggle API 설치

```bash
# venv 활성화
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# Kaggle API 설치
pip install kaggle
```

### 5단계: 설치 확인

```bash
kaggle --version
```

출력 예시:
```
Kaggle API 1.5.16
```

---

## 📥 자동 다운로드 스크립트 사용

### 방법 1: 전체 자동 다운로드 (권장)

```bash
# venv 활성화
venv\Scripts\activate

# 다운로드 스크립트 실행
python scripts/download_datasets.py
```

메뉴 선택:
```
1. GENKI-4K (필수) - 웃음 감지
2. FER2013 (필수) - 표정 분류
3. SMILES (선택) - 웃음 감지 보조
4. CelebA (선택) - 웃음 감지 대규모
5. 전체 다운로드 (GENKI-4K + FER2013)

선택 (1-5): 5
```

### 방법 2: Kaggle CLI 수동 다운로드

#### GENKI-4K

```bash
# 다운로드
kaggle datasets download -d talhasar/genki4k -p data/raw/genki4k

# 압축 해제
cd data/raw/genki4k
unzip genki4k.zip
del genki4k.zip  # Windows
rm genki4k.zip   # Linux/Mac
```

#### FER2013

```bash
# 다운로드
kaggle datasets download -d msambare/fer2013 -p data/raw/fer2013

# 압축 해제
cd data/raw/fer2013
unzip fer2013.zip
del fer2013.zip  # Windows
rm fer2013.zip   # Linux/Mac
```

#### CelebA (옵션)

```bash
# 주의: 약 1.4GB
kaggle datasets download -d jessicali9530/celeba-dataset -p data/raw/celeba

# 압축 해제
cd data/raw/celeba
unzip celeba-dataset.zip
del celeba-dataset.zip
```

---

## 📁 다운로드 후 디렉토리 구조

```
data/
├── raw/
│   ├── genki4k/
│   │   ├── files/           # 4,000장 이미지
│   │   └── labels.txt       # 라벨 파일
│   │
│   ├── fer2013/
│   │   ├── train/           # 학습 데이터
│   │   │   ├── angry/
│   │   │   ├── disgust/
│   │   │   ├── fear/
│   │   │   ├── happy/
│   │   │   ├── neutral/
│   │   │   ├── sad/
│   │   │   └── surprise/
│   │   ├── test/            # 테스트 데이터
│   │   └── validation/      # 검증 데이터
│   │
│   ├── smiles/ (선택)
│   │   ├── positives/       # 웃음 이미지
│   │   └── negatives/       # 비웃음 이미지
│   │
│   └── celeba/ (선택)
│       ├── img_align_celeba/  # 202,599장 이미지
│       └── list_attr_celeba.txt  # 속성 라벨
│
└── processed/  (학습 시 자동 생성)
```

---

## 🔍 다운로드 확인

### Python으로 확인

```python
from pathlib import Path

# GENKI-4K 확인
genki_path = Path("data/raw/genki4k")
if genki_path.exists():
    files = list(genki_path.rglob("*.jpg")) + list(genki_path.rglob("*.png"))
    print(f"GENKI-4K: {len(files)} 이미지")

# FER2013 확인
fer_path = Path("data/raw/fer2013")
if fer_path.exists():
    train_files = list((fer_path / "train").rglob("*.jpg"))
    print(f"FER2013: {len(train_files)} 학습 이미지")
```

### Bash로 확인

```bash
# GENKI-4K
find data/raw/genki4k -type f \( -name "*.jpg" -o -name "*.png" \) | wc -l

# FER2013
find data/raw/fer2013/train -type f -name "*.jpg" | wc -l
```

---

## 🚨 문제 해결

### 1. `kaggle: command not found`

**원인**: Kaggle API가 설치되지 않았거나 venv가 활성화되지 않음

**해결**:
```bash
# venv 활성화
venv\Scripts\activate

# Kaggle 설치
pip install kaggle
```

### 2. `Unauthorized: you must accept this dataset's terms`

**원인**: 데이터셋 이용약관 동의 필요

**해결**:
1. 브라우저에서 데이터셋 페이지 접속
2. "Download" 버튼 클릭하여 약관 동의
3. 다시 다운로드 시도

### 3. `Could not find kaggle.json`

**원인**: API 토큰이 올바른 위치에 없음

**해결**:
```bash
# Windows
echo %USERPROFILE%\.kaggle\kaggle.json

# 파일이 없으면 다시 복사
copy Downloads\kaggle.json %USERPROFILE%\.kaggle\kaggle.json
```

### 4. `403 Forbidden`

**원인**: API 토큰 권한 문제 (Linux/Mac)

**해결**:
```bash
chmod 600 ~/.kaggle/kaggle.json
```

### 5. 느린 다운로드 속도

**해결**:
- 안정적인 네트워크 사용
- VPN 끄기
- Kaggle 웹사이트에서 직접 다운로드 후 수동 복사

---

## 📊 데이터셋 통계

### GENKI-4K

| 항목 | 값 |
|------|-----|
| 총 이미지 수 | 4,000 |
| Smiling | ~2,000 |
| Non-smiling | ~2,000 |
| 해상도 | 다양 (평균 ~640x480) |
| 파일 형식 | JPG |
| 크기 | ~200 MB |

### FER2013

| 항목 | 값 |
|------|-----|
| 총 이미지 수 | 35,887 |
| Train | 28,709 |
| Validation | 3,589 |
| Test | 3,589 |
| 해상도 | 48x48 (그레이스케일) |
| 클래스 수 | 7 |
| 크기 | ~50 MB |

### CelebA (선택)

| 항목 | 값 |
|------|-----|
| 총 이미지 수 | 202,599 |
| Smiling | ~120,000 |
| Non-smiling | ~82,000 |
| 해상도 | 178x218 |
| 파일 형식 | JPG |
| 크기 | ~1.4 GB |

---

## 🎯 추천 다운로드 순서

### Phase 1: 최소 구성 (빠른 시작)

1. GENKI-4K (200MB) - 웃음 감지 학습
2. FER2013 (50MB) - 표정 분류 학습

**총 용량**: ~250 MB
**학습 시간**: 약 2-3시간 (RTX 4050 기준)

### Phase 2: 확장 구성

3. SMILES (선택) - 웃음 감지 성능 향상
4. CelebA (1.4GB) - 대규모 데이터로 성능 향상

**총 용량**: ~1.6 GB
**학습 시간**: 약 6-8시간

---

## 📝 다음 단계

1. ✅ Kaggle API 설정 완료
2. ✅ 데이터셋 다운로드 완료
3. ⬜ 데이터 전처리 파이프라인 구축
4. ⬜ 학습 스크립트 작성
5. ⬜ 모델 학습 시작

---

**작성자**: AI Development Team
**최종 업데이트**: 2026-01-14
